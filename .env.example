# =========================================================
# Gemini API設定（Google Cloud Console / Generative Language API）
# =========================================================
GEMINI_API_KEY=your_gemini_api_key_here
GOOGLE_API_VERSION=v1beta
DEFAULT_GEMINI_MODEL=gemini-2.5-flash
FALLBACK_GEMINI_MODEL=gemini-2.5-pro

# 開発用: Gemini APIに接続できない場合、モックを使用
USE_MOCK_GEMINI=false  # 本番環境ではfalseに設定

# =========================================================
# Web検索（Custom Search API）
# =========================================================
SEARCH_PROVIDER=google_cse
GOOGLE_API_KEY=your_google_search_api_key_here
GOOGLE_CSE_ID=your_custom_search_engine_id_here

# =========================================================
# 検索バイアス設定（信頼ドメイン）
# =========================================================
# 既定: true（日本語の書籍要約ではAmazon/版元/楽天/書評系を優先）
USE_TRUSTED_DOMAINS=true
# カスタムドメインで上書き可能（カンマ区切り）。未設定なら既定リストを使用。
# 例: TRUSTED_BOOK_DOMAINS=amazon.co.jp,hanmoto.com,books.rakuten.co.jp,bookmeter.com,booklog.jp,honz.jp
TRUSTED_BOOK_DOMAINS=

# =========================================================
# Redis設定
# =========================================================
# ローカル開発: redis://localhost:6379/0
# Render: Renderが自動設定するREDIS_URL環境変数を使用
REDIS_URL=redis://localhost:6379/0

# =========================================================
# データベース設定
# =========================================================
# ローカル開発: SQLiteが自動使用される
# Render: RenderのPostgreSQL DATABASE_URL が自動設定される
# DATABASE_URL=postgresql://user:password@host:port/dbname

# =========================================================
# セキュリティ設定
# =========================================================
# Flask SECRET_KEY (必須！本番環境では強力なランダム文字列を設定)
SECRET_KEY=your-super-secret-random-key-change-this-in-production

# HTTPS設定
FORCE_HTTPS=false  # ローカル開発環境ではfalse、本番環境ではtrue
SESSION_COOKIE_SECURE=false  # ローカル開発環境ではfalse、本番環境ではtrue
SESSION_COOKIE_HTTPONLY=true
SESSION_COOKIE_SAMESITE=Lax

# セッション有効期限（秒単位、デフォルト: 604800 = 7日間）
PERMANENT_SESSION_LIFETIME=604800

# =========================================================
# Gunicorn設定（本番環境）
# =========================================================
# ワーカー数（CPUコア数に応じて調整）
WEB_CONCURRENCY=2

# Render用: PORTは自動設定されるため不要
# PORT=8000
